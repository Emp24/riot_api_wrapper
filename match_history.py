"""objects created from match_history are list objects containing a list of all the matches played by a summoner,
it will contain the logic of creating a list of "match" objects from class "match" and adding them into a list"""
class match_history:
    def __init__(self):